# TODO — ADASFRO Blogger

Tareas identificadas para implementar a futuro (no urgentes para la tarea en curso). Se agregan acá durante el trabajo normal, según la regla de "Registro de tareas futuras" de `CLAUDE.md`.

> La sección "Problemas Conocidos / Deuda Técnica" de `CLAUDE.md` conserva el historial de deuda técnica anterior al 2026-08-30; desde esa fecha, los ítems nuevos se registran acá.

## Pendientes

- [ ] **`GEMINI.md` desactualizado** — sigue referenciando `swiper-fixes.css`, `donation-widget.js` y `swiper-init.js`, archivos ya eliminados del repo (Swiper y el widget de donaciones se retiraron, ver `CLAUDE.md` § Deuda Técnica). Actualizar para reflejar el estado actual (`theme.xml` + `neo-brutalist.css` + `blogger-fixes.css` + `accessibility-bar.css`/`.js` + `main.js`).
- [ ] **Archivos que superan el límite de 500 líneas** (restricción agregada 2026-08-30): `css/neo-brutalist.css`, `theme.xml`, `css/accessibility-bar.css`. Evaluar cómo modularizar sin romper el flujo de carga (un único XML de Blogger, CSS servido desde GitHub Pages). El includable propio de `Blog1` (2026-08-30) sumó ~65 líneas a `theme.xml`, agravando esto.
- [ ] **Verificar en Blogger real el includable propio de `Blog1`** (2026-08-30, `theme.xml` ~840-892): no se pudo probar contra el editor de Blogger, solo se validó la sintaxis XML por análisis. Antes de dar por cerrada la vista de Publicaciones en cards, confirmar en el preview de Blogger: (1) `data:post.thumbnailUrl` + `resizeImage(...)` devuelven la imagen recortada esperada (ver ítem siguiente — cambiado de `featuredImage` a `thumbnailUrl` el 2026-08-30, sin confirmar aún en Blogger real), (2) el badge de la primera etiqueta se ve bien cuando el post tiene varias, (3) `<b:include data='post' name='comments'/>` sigue mostrando/permitiendo comentarios en un post individual, (4) el paginador `nextprev` funciona en `/search`, (5) ya no aparece la imagen duplicada (gigante + chica) al entrar a un post individual — se quitó la inyección separada de `.post-single-image` el 2026-08-30.
- [ ] **Verificar en Blogger real el nuevo widget `Label1` (filtro por etiqueta)** (2026-08-30, `theme.xml` `#labels-filter`, ~823-837): primera vez que se agrega un `<b:widget type='Label'>` con includable propio en este tema — confirmar que Blogger lo aprovisiona correctamente al pegar el XML (a diferencia de Header1/PageList1/Blog1 que ya estaban en el tema original) y que `data:labels` trae las etiquetas reales del blog con sus URLs correctas a `/search/label/NOMBRE`.
- [ ] **Confirmar que los posts tengan las etiquetas de "Poblaciones que Atendemos"** (2026-08-30, `theme.xml` ~291-351): se corrigió un bug real en las URLs de "Ver entradas" (`+` en vez de `%20` para espacios en el path — Blogger nunca hacía match), pero incluso arreglado, el link solo muestra resultados si existen posts con la etiqueta exacta asignada (Discapacidad Física, Discapacidad Visual, Discapacidad Auditiva, Discapacidad Cognitiva, Neurodivergencia, Adultos Mayores, Población Migrante, Cuidadores, LGBTIQ). Si "Ver entradas" sigue vacío después del fix, es contenido faltante, no código.
- [ ] **Imágenes de las cards de Publicaciones no se ven** (reportado por el usuario 2026-08-30, tamaño ya corregido — sigue el problema de imágenes): se cambió el data tag de `data:post.featuredImage` a `data:post.thumbnailUrl` en las 4 apariciones (`theme.xml`) — la hipótesis es que `featuredImage` requiere que el post tenga marcada explícitamente una "imagen destacada" en el editor nuevo de Blogger, mientras que `thumbnailUrl` se autodetecta de cualquier imagen en el cuerpo del post, que es probablemente lo que sí tienen los posts de prueba. **No confirmado en Blogger real** — si sigue sin aparecer, verificar manualmente en el editor de Blogger si el post tiene alguna imagen en el cuerpo, y si el problema persiste con eso confirmado, escalar (puede requerir inspeccionar el HTML generado en vivo, algo que no se puede hacer desde este entorno).

## Completados

- [x] **Logos del carrusel "Aliados y colaboradores"** — el usuario confirmó que solo 6 convenios están confirmados (PANI, CONACIDA, CONAPDIS, FUNIBER, UH, MTSS); las otras 9 organizaciones que se habían agregado como placeholder el 2026-08-30 se eliminaron por no estar confirmadas. Las 6 restantes ya tienen logo real en `public/logos_convenio/`. Si se confirma un convenio nuevo a futuro, agregarlo con logo real desde el inicio (no placeholder de texto) en **ambas** copias del track (`theme.xml` `#aliados-marquee`).
